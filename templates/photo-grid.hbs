<div class="container photo-grid">
    {{#each instagramPosts }}
        {{#each this.images}}
            <div class="photo">
            <img src="./assets/images/{{this}}" alt="{{../caption}}" />
            {{!-- <p class="caption">{{../caption}}</p> --}}
            </div>
        {{/each}}
    {{/each}}
</div> 
